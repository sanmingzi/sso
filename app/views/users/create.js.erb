var div_flash = document.getElementById("flash");
div_flash.innerHTML = "<%= escape_javascript(render 'layouts/flash') %>";
<% flash.delete(:alert) %>

var form_user = document.getElementById('form_user');
form_user.innerHTML = "<%= escape_javascript(render 'form', user: @user) %>";
