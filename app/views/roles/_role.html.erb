<tr>
  <th scope='row'><%= role.id %></th>
  <td><%= role.name %></td>
  <td>
    <%= link_to "Edit", edit_role_path(role), class: 'btn btn-primary' %>
    <%= link_to "Authorize", authorize_role_path(role), class: 'btn btn-primary' %>
  </td>
</tr>
