<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <%= link_to 'SSO', root_path, class: "navbar-brand" %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item"><%= link_to 'Home', root_path, class: "nav-link" %></li>
      <% if is_login? %>
        <li class="nav-item"><%= link_to "Logout", logout_path, class: "nav-link" %></li>
        <li class="nav-item"><%= link_to "#{current_user.email}", user_path(current_user.id), class: "nav-link" %></li>
      <% else %>
        <li class="nav-item"><%= link_to "Login", login_path, class: "nav-link" %></li>
      <% end %>
    </ul>
  </div>
</nav>
