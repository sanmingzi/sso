var div_flash = document.getElementById("flash");
div_flash.innerHTML = "<%= escape_javascript(render 'layouts/flash') %>";
<% flash.delete(:alert) %>

var form_resource = document.getElementById("form_resource");
form_resource.innerHTML = "<%= escape_javascript(render 'form', resource: @resource) %>"
